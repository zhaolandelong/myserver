#!/usr/bin/env node
'use strict';
const app = require('../app.js');
const config = require('../config/config.js');
app.listen(config.port, () => {
    console.log('app stated, listening on port: ', config.port);
});
